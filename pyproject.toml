[project]
name = "maptap-utils"
version = "0.1.0"
description = "Data capture and processing scripts for maptap"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "pandas>=2.2.0",
    "pydantic>=2.7.0",
    "geopandas>=1.0.0",
    "shapely>=2.0.0",
]

[tool.uv]
package = false
